/**
 * Legacy backend removed.
 *
 * This project now uses Supabase (Postgres + Edge Functions + Auth)
 * as the only backend runtime.
 */

console.error('server.ts is deprecated. Use Vite for frontend and Supabase Edge Functions as backend.');
console.error('Run: npm run dev');
process.exit(1);
